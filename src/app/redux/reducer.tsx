const initState = {
  filters: {
    search: "",
    status: "All",
    priority: [],
  },
  todoList: [{}],
};

const rootReducer = (state = initState, action) => {
  switch (actions.type) {
    case "add":
      break;

    default:
      break;
  }
};
